docker-build-master:
  image: tmaier/docker-compose:latest
  stage: build
  services:
    - docker:dind
  before_script:
    - pwd
    - docker info  
  script: 
    - docker login -u "$DOCKER_USER" -p "$DOCKER_PASSWORD"
    - docker-compose build --pull
    - docker images
    - docker push devopslaravel_attendize_nginx
    - docker push devopslaravel_attendize_app 
    - docker logout
    
  tags:
    - dind
  only:
    - master
   
