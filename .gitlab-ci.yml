docker-build-master:
  image: tmaier/docker-compose:latest
  stage: build
  services:
    - docker:dind
  script:
    - pwd 
    - docker info
    - docker-compose --version
  tags:
    - dind
  only:
    - master

   